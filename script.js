document.addEventListener('DOMContentLoaded', () => { const scoreElement = document.getElementById('score-value'); const tappableObject = document.getElementById('tappable-object'); const tg = window.Telegram.WebApp; tg.ready(); tg.expand(); let score = 0; function handleTap(event) { score++; scoreElement.innerText = score; tappableObject.classList.add('tapped'); setTimeout(() => { tappableObject.classList.remove('tapped'); }, 100); event.preventDefault(); } tappableObject.addEventListener('touchstart', handleTap, { passive: false }); tappableObject.addEventListener('click', handleTap); });

